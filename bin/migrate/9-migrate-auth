#!/usr/bin/env bash

set -e

. $(PWD)/bin/init

# recycle auth
LANDBASE_AUTHORITY=$(dappCreate land contracts/LandBaseAuthority.sol:LandBaseAuthority [$MYSTERIOUSTREASURE_PROXY])
seth send $LANDBASE_PROXY "setAuthority(address)" $LANDBASE_AUTHORITY
